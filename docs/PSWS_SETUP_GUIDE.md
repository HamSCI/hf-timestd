# PSWS / GRAPE Upload Setup

PSWS/GRAPE upload workflows are handled by the separate `grape-recorder` project.

This repository (`hf-timestd`) does not generate Digital RF outputs and does not perform decimation.
