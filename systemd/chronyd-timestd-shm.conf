# Chrony service override for timestd SHM integration
# Ensure timestd-fusion starts before chronyd
# This allows fusion to create the SHM segment with correct permissions
# before chronyd tries to access it
[Unit]
After=timestd-fusion.service
Wants=timestd-fusion.service
