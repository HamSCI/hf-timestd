# hf-timestd - Core Dependencies
# For HF time standard recording/analysis from ka9q-radio RTP streams

# Configuration parsing
toml>=0.10.2

# Numerical computing and array operations
numpy>=1.24.0

# Signal processing
scipy>=1.10.0

# Audio file I/O (for compatibility with existing code)
soundfile>=0.12.0

# mDNS/Avahi service discovery for ka9q-radio streams
zeroconf>=0.132.0

# ka9q-radio control library (general-purpose radiod interface)
ka9q-python>=3.2.4

# WWV Timing Analysis & Visualization (optional, for plot_wwv_timing.py)
pandas>=2.0.0
matplotlib>=3.7.0

# IRI-2020 Ionospheric Model (optional, for improved propagation modeling)
# Requires: sudo apt install gfortran (before pip install)
# IRI-2020 provides better topside electron density, storm-time corrections,
# and high-latitude coverage compared to IRI-2016
iri2020 @ git+https://github.com/space-physics/iri2020.git

# PyLap - PHaRLAP Ray Tracing (optional, for physics-based propagation)
# Requires: Intel Fortran runtime libraries and PHaRLAP toolbox
# See: https://github.com/HamSCI/PyLap for installation instructions
# Provides 3D ray tracing through IRI ionosphere for accurate group delay
# pylap @ git+https://github.com/HamSCI/PyLap.git

# Compression (optional, for reduced disk I/O)
# Enable in timestd-config.toml: compression = "zstd" or "lz4"
# zstd: Best compression ratio (~2-3x), moderate CPU
# lz4: Fastest compression/decompression, lower ratio (~1.5-2x)
zstandard>=0.22.0
# lz4>=4.3.0

# Chrony SHM integration (for UTC(NIST) clock discipline)
sysv_ipc>=1.1.0
